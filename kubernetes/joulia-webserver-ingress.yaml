apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: joulia-webserver
spec:
  backend:
    serviceName: joulia-webserver
    servicePort: 80
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: joulia-webserver
          servicePort: 80
