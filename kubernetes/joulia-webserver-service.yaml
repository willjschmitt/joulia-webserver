apiVersion: v1
kind: Service
metadata:
  labels:
    app: joulia-webserver
  name: joulia-webserver
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8888
  selector:
    app: joulia-webserver
